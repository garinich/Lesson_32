@model BusinessLayer.ModelsDTO.BookDTO

@{
    ViewBag.Title = "Create New Book";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create New Book</h2>

@using (Html.BeginForm())
{
    <div class="row">
        <div class="form-group">
            <label>Book Name</label>
            @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" }})
            @*<input type="text" class="form-control" value="@Model.Book.Name" placeholder="Name" name="Name">*@
        </div>
        <div class="form-group">
            <label>Category</label>
            @Html.DropDownListFor(model => model.Category.Id,
                ViewBag.Categories as SelectList,
                new { Name = "CategoryId", @class = "form-control" })
            
            @*<select name="CategoryId" class="form-control">
                @foreach (var category in Model.Categories)
                {
                    <option value="@category.Id"
                            @if (Model.Book.Category.Id == category.Id)
                            {
                                Write("selected");
                            }>@category.Name</option>
                }
            </select>*@
        </div>
        <div class="form-group">
            <label>Writer</label>
            @Html.DropDownList("WriterIds",
                ViewBag.Writers as MultiSelectList,
                new { multiple = "true", @class = "form-control" })

            @*<select multiple name="WriterIds" class="form-control">
                @foreach (var writer in Model.Writers)
                {
                    <option value="@writer.Id"
                            @if (Model.Book.Writers.Select(wr => wr.Id).Contains(writer.Id))
                            {
                                Write("selected");
                            }>@writer.Name</option>
                }
            </select>*@
        </div>
        <button type="submit" class="btn btn-primary">Edit</button>
    </div>
}
